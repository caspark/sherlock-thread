#include <stdio.h>

int main (void) {
    puts("Hello, world via puts!");

    int a = 9;
    printf("Trying printf; it's over %u!!!", a * 1000);
    printf(" Or is it?\nOk, it's probably not.\n");
    return 0;
}
