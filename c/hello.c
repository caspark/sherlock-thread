#include <stdio.h>

int main (void) {
    puts("Hello, world\n(via puts!)");

    int a = 9;
    printf("Trying printf;\nit's over %u!!!", a * 1000);
    printf("Or is it?\nOk, it's probably not.\nOkay it's really not.");
    return 0;
}
